# Changelog

## Unreleased

- 插件设置增加流镜字号调整，仅缩放流镜内容与预览，设置页保持原字号；工具输入输出正文同步缩放。
- 工具详情默认展开输入，上方输入与下方输出按 40% / 60% 分配可用高度并独立滚动。
- 并发发送范围与任务标题单行显示，消息输入框恢复边框；帮助说明保留逐条换行。
- 修复设置规则卡片被预览文字撑高；统一 Flowglass 根字号、单行 header、搜索/回放浮层和会话标签换行。
- “带入”、节点标记、header 辅助操作采用图标与 tooltip，保留无障碍名称与主要操作文字。
- 概览直接显示最近执行步骤；概览/对比采用同一主滚动容器，默认看最新，向上阅读时刷新保持位置。
- 发送成功后收起开工区，持久化当前任务范围；带入当前会话草稿不再重复导航。修复展开筛选误触开工区收起。

- 修复并发模型/思考强度的空选项、刷新时选择被旧值覆盖，以及冷会话工作目录查询导致的历史范围错误。
- 当前会话恢复固定三列：左侧子代理、中间主干、右侧工具；窄侧栏保留横向滚动，不再把子代理移到流程底部。
- 关系图用任务输入与提交时间定位实际日志轮次，避免引用分叉前的旧回答；兼容当前 Harness 冷分叉日志读取异常。
- 导航统一为“当前会话 / 并发任务”和“概览 / 对比 / 关系图”；增加上下文面包屑、安静的技术信息和默认折叠的系统上下文。
- 按面板容器宽度布局，提供键盘可操作的卡片、返回详情、宽栏并排 Inspector、主题与降低动效支持。
- 增加已加载 / 全会话查找、类型与状态筛选、需要关注入口、带备注的本地节点标记。
- 并发概览显示分支最新结论；对比只在输入证据充分时对齐，支持共享输入、差异过滤和带来源的结论草稿预览。
- 关系图增加适应画布、定位、恢复布局、缩放和平移；增加只读事件回放和可预览的本地 JSON / SVG 导出。
- Composer 与 Inspector 使用稳定 React 区域；分支发送记录 create/configure/send 和成功 / 失败 / 未知结果，恢复时复用已有 Session。
- 统一静态包与源码动态 Client 组装，保留普通 Toolbox HTML 面板，增加完整工具箱与 flowedit 样式回归。
- 对重复的持久事件投影增加有界缓存；live、原始瞬时 chunk 和超大日志绕过，覆盖内容修改、回放和驱逐回归。
- 修复临时 Host 状态残留导致旧版 losslessJSON RPC 校验失败的问题。

实现与限制见 [实施记录](docs/flowglass-ux-implementation.md)，原离线验证见 [基线记录](docs/flowglass-ux-baseline.md)，后续真实实例修复见 [3080 验收记录](docs/flowglass-live-fixes-2026-09-20.md)。已更新本机 3080 的安装文件，尚未发布新版本。

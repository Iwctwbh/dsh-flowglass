# 工具箱运行状态 + 内容产物（Jira 查询记录、AI 台账、启停记忆、自动补齐报告、工单归档附件等，勿入库）
.dsh-dynamic-toolbox/
# 开发草稿/一次性脚本临时目录（正式资产在 smoke/）
.scratch/
# 凭据文件（Jira 凭据来源之一是项目 .env，绝不入库）
.env
.env.*
# 依赖目录（本仓库零 npm 依赖；防止本地试验误入库）
node_modules/
# 系统/编辑器杂项
Thumbs.db
Desktop.ini
*.log
# DSH 源码/安装副本（嵌套 Git 仓库，仅本地参考，勿入库）
deepseek-harness/
# 编译合集产物（npm bundle 生成物，dist 整目录不入库；发布走 npm pack/CI artifact）
dist/

# 本机项目记忆指针，不进入公开仓库
.memory-project.json
